
{
  "types": [
    { "id":5, "name":"Date", "color":"orange", "init":"new Date()" }
  ],
  "nodes": [
    {
      "name": "Date Now",
      "type": 5,
      "version": 1,
      "inner_text": "<i class=\"icon i-clock\"></i>",
      "import": ["java.util.Date"],
      "java": "new Date()",
      "input": [
      ],
      "output": [
        { "type": "Date", "label": "Now"}
      ]
    },
    {
      "name": "Date to String",
      "type": 4,
      "version": 1,
      "import": ["java.util.Date"],
      "java": "out{1} = in{1}.toString();",
      "input": [
        { "type": "Exec", "label": ""},
        { "type": "Date", "label": "Date"}
      ],
      "output": [
        { "type": "Exec", "label": ""},
        { "type": "String", "label": "", "java":{"references":{"variable":"date_str"}}}
      ]
    },
    {
      "name": "Date Parse",
      "type": 4,
      "version": 1,
      "import": ["java.util.Date", "java.text.DateFormat", "java.text.ParseException"],
      "java": "boolean _{node.id}_parse_ok = false; try { DateFormat df = DateFormat.getDateInstance(); out{1} = df.parse(in{1}); _{node.id}_parse_ok = true; } catch (ParseException e) { _{node.id}_parse_ok = false; } if (_{node.id}_parse_ok) { exec{0} } else { exec{2}}",
      "input": [
        { "type": "Exec", "label": ""},
        { "type": "String", "label": "String", "value":"", "single_line":true }
      ],
      "output": [
        { "type": "Exec", "label": "Success"},
        { "type": "Date", "label": "Date", "java":{"references":{"variable":"date"}}},
        { "type": "Exec", "label": "Error"}
      ]
    }
  ]
}
